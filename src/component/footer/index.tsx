export default function Footer() {
  return (
    <footer className="pxl-footer px-5 py-8 text-center text-xs">
      <p>
        © {new Date().getFullYear()} pixelonTech — todos os pixels reservados.
      </p>
    </footer>
  );
}
